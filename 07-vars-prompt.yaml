- name: print variables though prompt
  hosts: web
  vars_prompt:
  - name: COURSE
    prompt: Please enter the course name
    private: no
  - name: PLATFORM
    prompt: Please enetr duration
  tasks:
  - name: prompt variables
    ansible.builtin.debug:
      msg: "Hi, I am leaning {{COURSE}} course, platform is {{PLATFORM}}"